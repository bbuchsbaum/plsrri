---
phase: 02-business-logic-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/shiny/R/fct_brain_viewer.R
  - inst/shiny/R/mod_brain_viewer.R
  - inst/shiny/R/mod_inspector.R
  - tests/testthat/test-fct_brain_viewer.R
autonomous: true

must_haves:
  truths:
    - "Brain viewer computations run without Shiny context"
    - "Modules delegate to fct_* functions for data transformations"
    - "Unit tests pass without testServer()"
  artifacts:
    - path: "inst/shiny/R/fct_brain_viewer.R"
      provides: "Pure functions for brain viewer computations"
      exports: ["get_filter_defaults", "format_colorbar_label", "compute_plot_height", "compute_bsr_summary", "compute_lv_stats", "build_lv_choices"]
    - path: "tests/testthat/test-fct_brain_viewer.R"
      provides: "Unit tests for brain viewer functions"
      min_tests: 20
  key_links:
    - from: "inst/shiny/R/mod_brain_viewer.R"
      to: "fct_brain_viewer.R"
      via: "function calls in renderPlot"
      pattern: "get_filter_defaults|format_colorbar_label"
    - from: "inst/shiny/R/mod_inspector.R"
      to: "fct_brain_viewer.R"
      via: "function calls in renderUI"
      pattern: "compute_bsr_summary|compute_lv_stats"
---

<objective>
Extract brain viewer business logic from mod_brain_viewer.R and mod_inspector.R into fct_brain_viewer.R

Purpose: Create unit-testable pure functions for brain visualization computations
Output: fct_brain_viewer.R with computation functions, unit tests, and module delegation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-logic-extraction/02-CONTEXT.md

@inst/shiny/R/mod_brain_viewer.R
@inst/shiny/R/mod_inspector.R
@inst/shiny/R/mod_filter_bar.R
@tests/testthat/test-mod_brain_viewer.R
@tests/testthat/test-mod_inspector.R
@tests/testthat/helper-fixtures.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fct_brain_viewer.R with pure computation functions</name>
  <files>inst/shiny/R/fct_brain_viewer.R</files>
  <action>
Create fct_brain_viewer.R in inst/shiny/R/ with these pure functions:

1. `get_filter_defaults(filters, defaults = list(lv = 1L, bsr_threshold = 3.0, what = "bsr"))` -> list
   - Takes a filters list (may have NULL values) and returns list with defaults applied
   - Extracted from mod_brain_viewer.R lines 143-149
   - Example: get_filter_defaults(list(lv = NULL, bsr_threshold = 2.0)) -> list(lv = 1L, bsr_threshold = 2.0, what = "bsr")

2. `format_colorbar_label(what, threshold)` -> character
   - what: "bsr" or "salience"
   - Returns descriptive label for colorbar
   - "bsr" -> sprintf("Bootstrap Ratio (|BSR| > %.1f)", threshold)
   - "salience" -> "Salience weights"
   - Extracted from mod_brain_viewer.R lines 183-188

3. `compute_plot_height(view_mode)` -> integer
   - "ortho" -> 400L, "montage" -> 350L
   - Extracted from mod_brain_viewer.R lines 166-173

4. `compute_bsr_summary(bsr_values, threshold = 3)` -> list or NULL
   - Takes numeric vector of BSR values
   - Returns list(n_pos, n_neg, max_pos, max_neg) counting values above/below threshold
   - Returns NULL if bsr_values is NULL
   - Extracted from mod_inspector.R lines 104-114

5. `compute_lv_stats(singular_values, lv_index)` -> list
   - singular_values: numeric vector of singular values (result$s)
   - lv_index: which LV to compute stats for
   - Returns list(var_explained, cum_var_explained) as percentages
   - Extracted from mod_inspector.R lines 92-94

6. `build_lv_choices(n_lv, p_values = NULL)` -> named character vector
   - n_lv: number of latent variables
   - p_values: optional vector of p-values (from perm_result$sprob)
   - Returns choices vector for selectInput: c("All" = "all", "LV1 *" = "1", "LV2" = "2", ...)
   - Asterisk added for significant (p < 0.05) LVs
   - Extracted from mod_filter_bar.R lines 131-148

All functions should:
- Take plain R objects as parameters (not reactives)
- Return plain R objects
- Handle NULL/NA inputs gracefully
- Have roxygen2 documentation with @keywords internal
  </action>
  <verify>
File exists: `test -f inst/shiny/R/fct_brain_viewer.R`
File sources without error: `Rscript -e "source('inst/shiny/R/fct_brain_viewer.R')"`
  </verify>
  <done>
fct_brain_viewer.R contains 6 pure functions with roxygen2 documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for fct_brain_viewer.R</name>
  <files>tests/testthat/test-fct_brain_viewer.R</files>
  <action>
Create test-fct_brain_viewer.R with comprehensive unit tests:

```r
# Test fct_brain_viewer pure functions
# These tests run WITHOUT Shiny context - plain testthat

# Source the functions directly
source(file.path("inst/shiny/R", "fct_brain_viewer.R"))
```

Test cases for get_filter_defaults():
- All NULLs returns all defaults
- Partial values keeps provided, fills NULLs
- All values provided returns as-is
- Empty filters list returns all defaults

Test cases for format_colorbar_label():
- "bsr" with threshold 3.0 -> "Bootstrap Ratio (|BSR| > 3.0)"
- "bsr" with threshold 2.5 -> "Bootstrap Ratio (|BSR| > 2.5)"
- "salience" -> "Salience weights"
- Unknown what defaults reasonably

Test cases for compute_plot_height():
- "ortho" -> 400
- "montage" -> 350
- Unknown mode defaults to 350

Test cases for compute_bsr_summary():
- NULL input returns NULL
- All zeros returns n_pos=0, n_neg=0
- Values above threshold counted in n_pos
- Values below -threshold counted in n_neg
- Max/min computed correctly
- NA values handled (na.rm)

Test cases for compute_lv_stats():
- Single LV returns 100% for both
- Multiple LVs compute variance correctly (s^2/sum(s^2)*100)
- Cumulative variance is cumsum
- Edge case: lv_index = 1

Test cases for build_lv_choices():
- n_lv=1 returns c("All"="all", "LV1"="1")
- n_lv=3 returns 4 choices (all + 3 LVs)
- With p_values, significant LVs get asterisk
- p_values=NULL means no asterisks
- p_value exactly 0.05 is not significant (< 0.05 required)

Use describe() blocks. Target 20+ test cases.
  </action>
  <verify>
Run tests: `Rscript -e "testthat::test_file('tests/testthat/test-fct_brain_viewer.R')"`
All tests pass
  </verify>
  <done>
test-fct_brain_viewer.R has 20+ passing unit tests covering all brain viewer functions
  </done>
</task>

<task type="auto">
  <name>Task 3: Update modules to delegate to fct_brain_viewer.R</name>
  <files>inst/shiny/R/mod_brain_viewer.R, inst/shiny/R/mod_inspector.R, inst/shiny/R/mod_filter_bar.R</files>
  <action>
Update modules to use fct_brain_viewer.R functions:

**mod_brain_viewer.R:**

1. Source fct_brain_viewer.R at module load time

2. In renderPlot (brain_plot, around line 126):
   - Replace inline filter extraction with:
     ```r
     filter_vals <- get_filter_defaults(list(
       lv = if (!is.null(filters$lv)) filters$lv() else NULL,
       bsr_threshold = if (!is.null(filters$bsr_threshold)) filters$bsr_threshold() else NULL,
       what = if (!is.null(filters$what)) filters$what() else NULL
     ))
     lv <- filter_vals$lv
     threshold <- filter_vals$bsr_threshold
     what <- filter_vals$what
     ```

3. In renderUI (colorbar_info, around line 176):
   - Replace inline label logic with: format_colorbar_label(what, threshold)

4. In renderPlot height function (around line 166):
   - Replace inline switch with: compute_plot_height(local_rv$view_mode)

**mod_inspector.R:**

1. Source fct_brain_viewer.R at module load time

2. In renderUI (lv_details, around line 83):
   - Replace inline variance calculation with:
     ```r
     lv_stats <- compute_lv_stats(result$s, lv)
     var_exp <- lv_stats$var_explained
     cum_var <- lv_stats$cum_var_explained
     ```
   - Replace BSR summary calculation with:
     ```r
     bsr_summary <- if (!is.null(result$boot_result)) {
       bsr_vals <- plsrri::bsr(result, lv = lv)
       compute_bsr_summary(bsr_vals, threshold = 3)
     } else NULL
     ```

**mod_filter_bar.R:**

1. Source fct_brain_viewer.R at module load time

2. In observe (LV choices update, around line 127):
   - Replace inline choices building with:
     ```r
     p_vals <- if (!is.null(result$perm_result)) result$perm_result$sprob else NULL
     choices <- build_lv_choices(n_lv, p_vals)
     ```

Keep reactive wrappers intact - only replace computation logic.
  </action>
  <verify>
- Existing brain_viewer tests pass: `Rscript -e "testthat::test_file('tests/testthat/test-mod_brain_viewer.R')"`
- Existing inspector tests pass: `Rscript -e "testthat::test_file('tests/testthat/test-mod_inspector.R')"`
- Full test suite: `Rscript -e "devtools::test()"`
  </verify>
  <done>
mod_brain_viewer.R, mod_inspector.R, and mod_filter_bar.R delegate to fct_brain_viewer.R; all existing tests pass
  </done>
</task>

</tasks>

<verification>
1. New unit tests pass: `Rscript -e "testthat::test_file('tests/testthat/test-fct_brain_viewer.R')"`
2. Existing module tests pass: `Rscript -e "testthat::test_file('tests/testthat/test-mod_brain_viewer.R')"`
3. Full test suite passes: `Rscript -e "devtools::test()"`
4. App loads without errors: `Rscript -e "shiny::runApp('inst/shiny', launch.browser = FALSE)"`
</verification>

<success_criteria>
- fct_brain_viewer.R exists with 6 pure functions
- Unit tests exist with 20+ test cases, all passing
- mod_brain_viewer.R, mod_inspector.R, mod_filter_bar.R call fct_* functions
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-extraction/02-02-SUMMARY.md`
</output>
