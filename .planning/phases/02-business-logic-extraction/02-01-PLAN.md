---
phase: 02-business-logic-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/shiny/R/fct_data_validation.R
  - inst/shiny/R/mod_setup.R
  - tests/testthat/test-fct_data_validation.R
autonomous: true

must_haves:
  truths:
    - "Validation logic runs without Shiny context"
    - "Module delegates to fct_* functions for validation"
    - "Unit tests pass without testServer()"
  artifacts:
    - path: "inst/shiny/R/fct_data_validation.R"
      provides: "Pure validation functions"
      exports: ["validate_setup_config", "validate_groups", "validate_data_source", "map_method_to_int"]
    - path: "tests/testthat/test-fct_data_validation.R"
      provides: "Unit tests for validation functions"
      min_tests: 15
  key_links:
    - from: "inst/shiny/R/mod_setup.R"
      to: "fct_data_validation.R"
      via: "function calls in validate_setup reactive"
      pattern: "validate_setup_config|validate_groups"
---

<objective>
Extract data validation business logic from mod_setup.R into fct_data_validation.R

Purpose: Create unit-testable pure functions for setup validation, removing business logic from reactive context
Output: fct_data_validation.R with validation functions, unit tests, and module delegation
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-business-logic-extraction/02-CONTEXT.md

@inst/shiny/R/mod_setup.R
@tests/testthat/test-mod_setup.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fct_data_validation.R with pure validation functions</name>
  <files>inst/shiny/R/fct_data_validation.R</files>
  <action>
Create fct_data_validation.R in inst/shiny/R/ with these pure functions extracted from mod_setup.R:

1. `validate_setup_config(data_source, data_loaded, bids_dir, groups, num_conditions, num_boot)` -> character vector of errors
   - Extract logic from lines 532-568 of mod_setup.R
   - Check data source validity (manual needs data_loaded, bids needs dir selected)
   - Check group count (>= 1 group)
   - Check subject counts (>= 1 per group)
   - Check conditions (>= 1)
   - Check bootstrap requirements (>= 3 subjects if num_boot > 0)
   - Return character(0) if valid, otherwise vector of error messages

2. `validate_groups(groups)` -> character vector of errors
   - groups is list of list(name, n_subj)
   - Check length >= 1
   - Check all n_subj >= 1
   - Return specific error messages

3. `validate_data_source(data_source, data_loaded, bids_dir)` -> character vector of errors
   - Check "manual" requires data_loaded == TRUE
   - Check "bids" requires bids_dir is not integer (meaning selected)

4. `map_method_to_int(method)` -> integer
   - Extract logic from lines 609-616
   - "task" -> 1L, "behavior" -> 3L, "multiblock" -> 4L

5. `parse_uploaded_file(file_path, file_name)` -> matrix or NULL
   - Extract logic from lines 303-316
   - Handle .csv, .rds, .rda extensions
   - Return matrix if successful, NULL otherwise

All functions should:
- Take plain R objects as parameters (not reactives)
- Return plain R objects (not reactive values)
- Have roxygen2 documentation
- Be prefixed with package-internal @keywords internal

Follow existing R code style in the shiny/R/ directory.
  </action>
  <verify>
File exists: `test -f inst/shiny/R/fct_data_validation.R`
File sources without error: `Rscript -e "source('inst/shiny/R/fct_data_validation.R')"`
  </verify>
  <done>
fct_data_validation.R contains 5 pure functions with roxygen2 documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for fct_data_validation.R</name>
  <files>tests/testthat/test-fct_data_validation.R</files>
  <action>
Create test-fct_data_validation.R with comprehensive unit tests:

```r
# Test fct_data_validation pure functions
# These tests run WITHOUT Shiny context - plain testthat

# Source the functions directly (they're internal to shiny app)
source(file.path("inst/shiny/R", "fct_data_validation.R"))
```

Test cases for validate_setup_config():
- Valid config returns character(0)
- Missing data for manual source returns error
- Missing BIDS dir returns error
- Zero groups returns error
- Zero subjects in group returns error
- Zero conditions returns error
- Bootstrap with <3 subjects returns error
- Multiple errors returned together

Test cases for validate_groups():
- Valid groups list returns character(0)
- Empty groups list returns error
- Group with 0 subjects returns error
- Group with negative subjects returns error

Test cases for validate_data_source():
- Manual with data_loaded=TRUE returns character(0)
- Manual with data_loaded=FALSE returns error
- BIDS with valid dir returns character(0)
- BIDS with integer dir returns error

Test cases for map_method_to_int():
- "task" -> 1L
- "behavior" -> 3L
- "multiblock" -> 4L
- Unknown method -> 1L (default)

Test cases for parse_uploaded_file():
- .csv file parses to matrix
- .rds file parses to matrix
- Invalid file returns NULL
- Use tempfile() for test data

Use describe() blocks for organization. Target 15+ test cases total.
  </action>
  <verify>
Run tests: `Rscript -e "testthat::test_file('tests/testthat/test-fct_data_validation.R')"`
All tests pass
  </verify>
  <done>
test-fct_data_validation.R has 15+ passing unit tests covering all validation functions
  </done>
</task>

<task type="auto">
  <name>Task 3: Update mod_setup.R to delegate to fct_* functions</name>
  <files>inst/shiny/R/mod_setup.R</files>
  <action>
Modify mod_setup.R to use fct_data_validation.R functions:

1. At top of file, after existing sources, add:
   `source("fct_data_validation.R", local = TRUE)`
   (or appropriate relative path based on how Shiny app loads modules)

2. Replace validate_setup() reactive (lines 532-568):
   - Keep the reactive wrapper
   - Replace inline logic with call to validate_setup_config()
   - Pass current values: input$data_source, local_rv$data_loaded, input$bids_dir, local_rv$groups, input$num_conditions, input$num_boot

3. Replace method mapping in continue handler (lines 609-616):
   - Replace inline switch with call to map_method_to_int(input$method)

4. Replace file parsing in observeEvent(input$data_files, {...}) (lines 297-342):
   - Replace inline tryCatch/if-else with call to parse_uploaded_file()
   - Keep reactive wrapper and error handling

The reactive layer should become thin:
- Reactive expressions handle timing (when to run)
- fct_* functions handle computation (what to compute)

Keep all existing reactive wiring intact - only replace the computation logic.
  </action>
  <verify>
- Existing module tests still pass: `Rscript -e "testthat::test_file('tests/testthat/test-mod_setup.R')"`
- App still loads: `Rscript -e "shiny::runApp('inst/shiny', launch.browser = FALSE)"`
  </verify>
  <done>
mod_setup.R delegates to fct_data_validation.R for validation, method mapping, and file parsing; existing tests pass
  </done>
</task>

</tasks>

<verification>
1. New unit tests pass without Shiny context: `Rscript -e "testthat::test_file('tests/testthat/test-fct_data_validation.R')"`
2. Existing module tests pass: `Rscript -e "testthat::test_file('tests/testthat/test-mod_setup.R')"`
3. Full test suite passes: `Rscript -e "devtools::test()"`
</verification>

<success_criteria>
- fct_data_validation.R exists with 5 pure functions
- Unit tests exist with 15+ test cases, all passing
- mod_setup.R calls fct_* functions instead of containing business logic inline
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-business-logic-extraction/02-01-SUMMARY.md`
</output>
