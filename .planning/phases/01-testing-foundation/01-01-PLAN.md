---
phase: 01-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/helper-fixtures.R
  - tests/testthat/fixtures/pls_result_basic.rds
  - tests/testthat/fixtures/pls_result_with_boot.rds
  - tests/testthat/fixtures/pls_result_with_perm.rds
  - tests/testthat/fixtures/pls_result_full.rds
autonomous: true

must_haves:
  truths:
    - "Calling make_mock_pls_result() returns a valid pls_result object"
    - "Fixtures load successfully with readRDS()"
    - "Fixtures have correct class and required slots"
    - "Variant fixtures differ in expected ways (boot_result present/absent)"
  artifacts:
    - path: "tests/testthat/helper-fixtures.R"
      provides: "Factory functions for mock pls_result objects"
      exports: ["make_mock_pls_result", "make_mock_spec", "make_mock_mask"]
    - path: "tests/testthat/fixtures/pls_result_basic.rds"
      provides: "Minimal pls_result (no resampling)"
    - path: "tests/testthat/fixtures/pls_result_with_boot.rds"
      provides: "pls_result with bootstrap only"
    - path: "tests/testthat/fixtures/pls_result_with_perm.rds"
      provides: "pls_result with permutation only"
    - path: "tests/testthat/fixtures/pls_result_full.rds"
      provides: "pls_result with both bootstrap and permutation"
  key_links:
    - from: "helper-fixtures.R"
      to: "R/classes.R"
      via: "Uses new_pls_result, new_pls_boot_result, new_pls_perm_result"
      pattern: "new_pls_result|new_pls_boot_result|new_pls_perm_result"
---

<objective>
Create mock pls_result fixtures and factory functions for Shiny module testing.

Purpose: Module tests need realistic pls_result objects to verify reactive logic. These fixtures enable testing without running actual PLS analyses (which are slow and require real data).

Output: Helper file with fixture factory functions + pre-generated .rds fixtures with structural variants (with/without bootstrap, with/without permutation).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-foundation/01-CONTEXT.md

# Key source files
@R/classes.R (pls_result, pls_boot_result, pls_perm_result constructors)
@tests/testthat/test-accessors.R (example of creating test data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fixture helper functions</name>
  <files>tests/testthat/helper-fixtures.R</files>
  <action>
Create tests/testthat/helper-fixtures.R with factory functions for mock pls_result objects.

Include these functions:

1. `make_mock_mask(dim = c(10, 10, 10))` — Creates a minimal NeuroVol mask
   - Use neuroim2::NeuroVol if available, else create a simple array with class
   - Should have ~500 "brain" voxels (half the volume)

2. `make_mock_pls_result(n_voxels = 500, n_lv = 3, n_obs = 30, include_boot = FALSE, include_perm = FALSE, include_mask = FALSE)` — Main factory
   - Create u matrix (n_voxels x n_lv) with random values
   - Create s vector (n_lv) with descending positive values
   - Create v matrix (n_obs x n_lv) with random values
   - If include_boot: create boot_result with compare_u (BSR), u_se
   - If include_perm: create perm_result with sprob (p-values)
   - If include_mask: attach make_mock_mask() result
   - Use plsrri:::new_pls_result() constructor

3. `make_mock_spec(n_groups = 1, n_subj = 10, n_cond = 3, n_voxels = 500)` — For setup module
   - Return valid pls_spec object with data matrices

4. Variants for edge cases:
   - `make_single_lv_result()` — only 1 LV
   - `make_many_lv_result()` — 10+ LVs

Use set.seed() inside each function for reproducibility.
  </action>
  <verify>
Source the helper file in R and verify:
- `make_mock_pls_result()` returns object with class "pls_result"
- `make_mock_pls_result(include_boot = TRUE)$boot_result` is not NULL
- `make_mock_pls_result(include_perm = TRUE)$perm_result` is not NULL
  </verify>
  <done>
Helper file exists at tests/testthat/helper-fixtures.R with all factory functions. Functions return correctly structured objects matching pls_result class.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate pre-built fixture files</name>
  <files>
tests/testthat/fixtures/pls_result_basic.rds
tests/testthat/fixtures/pls_result_with_boot.rds
tests/testthat/fixtures/pls_result_with_perm.rds
tests/testthat/fixtures/pls_result_full.rds
  </files>
  <action>
Create fixtures/ directory under tests/testthat/ and generate 4 pre-built fixture files.

Using the helper functions from Task 1:

1. `pls_result_basic.rds` — Basic result, no resampling
   - make_mock_pls_result(n_voxels = 500, n_lv = 3, n_obs = 30)

2. `pls_result_with_boot.rds` — With bootstrap only
   - make_mock_pls_result(include_boot = TRUE)

3. `pls_result_with_perm.rds` — With permutation only
   - make_mock_pls_result(include_perm = TRUE)

4. `pls_result_full.rds` — Full result with both
   - make_mock_pls_result(include_boot = TRUE, include_perm = TRUE, include_mask = TRUE)

Create a small R script or run interactively to generate these, then save with saveRDS().

Also update helper-fixtures.R to include a load_fixture() convenience function:
```r
load_fixture <- function(name) {

  path <- testthat::test_path("fixtures", paste0(name, ".rds"))
  readRDS(path)
}
```
  </action>
  <verify>
- Directory tests/testthat/fixtures/ exists
- All 4 .rds files exist and are non-empty
- Each file loads with readRDS() and returns pls_result class
- pls_result_with_boot.rds has non-NULL boot_result
- pls_result_with_perm.rds has non-NULL perm_result
  </verify>
  <done>
Four fixture files exist in tests/testthat/fixtures/. Each loads correctly and has the expected structural properties (boot/perm presence varies as intended).
  </done>
</task>

<task type="auto">
  <name>Task 3: Add fixture validation tests</name>
  <files>tests/testthat/test-fixtures.R</files>
  <action>
Create tests/testthat/test-fixtures.R to verify fixture integrity.

Tests should confirm:

1. All pre-built fixtures load and have correct class
2. Factory functions produce valid objects
3. Structural properties are correct:
   - dim(result$u) matches expected
   - length(result$s) equals n_lv
   - boot_result presence matches include_boot parameter
   - perm_result presence matches include_perm parameter
4. Edge case fixtures work (single LV, many LVs)
5. Fixtures work with accessor functions (salience(), bsr(), scores())

Use describe() blocks to organize:
- describe("Pre-built fixtures")
- describe("Factory functions")
- describe("Edge cases")
  </action>
  <verify>
Run `devtools::test(filter = "fixtures")` — all tests pass
  </verify>
  <done>
test-fixtures.R exists with comprehensive tests. Running devtools::test(filter = "fixtures") shows all tests passing.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

```r
# In R console from package root
devtools::load_all()
devtools::test(filter = "fixtures")

# Verify helper file auto-loads (testthat convention)
# Helper files in tests/testthat/helper-*.R are auto-sourced
testthat::test_that("helpers load", {
  testthat::expect_true(exists("make_mock_pls_result"))
})
```

All fixture tests should pass. Factory functions should be available in test context.
</verification>

<success_criteria>
- [ ] tests/testthat/helper-fixtures.R exists with factory functions
- [ ] tests/testthat/fixtures/ directory contains 4 .rds files
- [ ] Factory functions create valid pls_result objects
- [ ] Pre-built fixtures load correctly
- [ ] Fixtures have structural variants (boot/perm presence varies)
- [ ] test-fixtures.R passes with devtools::test(filter = "fixtures")
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-01-SUMMARY.md`
</output>
