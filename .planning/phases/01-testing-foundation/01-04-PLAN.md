---
phase: 01-testing-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/testthat/test-mod_explore.R
  - tests/testthat/test-mod_brain_viewer.R
  - tests/testthat/test-mod_inspector.R
autonomous: true

must_haves:
  truths:
    - "testServer() tests exist for mod_explore reactive logic"
    - "testServer() tests exist for mod_brain_viewer reactive logic"
    - "testServer() tests exist for mod_inspector reactive logic"
    - "Tests use mock pls_result fixtures for result-dependent behavior"
  artifacts:
    - path: "tests/testthat/test-mod_explore.R"
      provides: "Explore module server tests"
      contains: "testServer"
    - path: "tests/testthat/test-mod_brain_viewer.R"
      provides: "Brain viewer module server tests"
      contains: "testServer"
    - path: "tests/testthat/test-mod_inspector.R"
      provides: "Inspector module server tests"
      contains: "testServer"
  key_links:
    - from: "test-mod_explore.R"
      to: "inst/shiny/R/mod_explore.R"
      via: "testServer calls explore_server"
      pattern: "explore_server"
    - from: "test-mod_brain_viewer.R"
      to: "helper-fixtures.R"
      via: "uses load_fixture or make_mock_pls_result"
      pattern: "make_mock_pls_result|load_fixture"
---

<objective>
Create testServer() tests for mod_explore, mod_brain_viewer, and mod_inspector modules.

Purpose: Verify reactive server logic for the more complex result exploration modules. These modules depend heavily on pls_result objects, making the fixtures from Plan 01 essential.

Output: Three test files covering the Explore, Brain Viewer, and Inspector modules' reactive logic with various fixture scenarios.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-foundation/01-CONTEXT.md

# Dependencies from Wave 1
@.planning/phases/01-testing-foundation/01-01-SUMMARY.md (if exists)

# Module source files
@inst/shiny/R/mod_explore.R
@inst/shiny/R/mod_brain_viewer.R
@inst/shiny/R/mod_inspector.R
@inst/shiny/R/state.R

# Fixture helpers
@tests/testthat/helper-fixtures.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mod_explore testServer tests</name>
  <files>tests/testthat/test-mod_explore.R</files>
  <action>
Create tests/testthat/test-mod_explore.R with testServer() tests for the explore module.

**Module complexity:** mod_explore orchestrates filter_bar, brain_viewer, and inspector as child modules. Testing focuses on the parent module's coordination logic.

**Tests to include:**

1. `describe("explore_server initialization")`:
   - Module initializes with selected_lv = 1
   - Child modules (filters, brain, inspector) are callable

2. `describe("explore_server LV selection")`:
   - Clicking LV item updates selected_lv
   - Filter bar LV change syncs to local state
   - LV list shows correct number of items from result

3. `describe("explore_server with different results")`:
   - Works with result that has perm_result (shows significance)
   - Works with result without perm_result
   - Works with result that has many LVs

4. `describe("explore_server variance calculation")`:
   - Variance explained percentages are correct
   - Cumulative variance is computed

**Test setup:**
```r
# Create state_rv with a result for explore module
make_explore_test_state <- function(result = NULL) {
  if (is.null(result)) {
    result <- make_mock_pls_result(include_perm = TRUE, include_boot = TRUE)
  }
  reactiveValues(
    step = 3L,
    max_step = 3L,
    spec = make_mock_spec(),
    result = result,
    selected_lv = 1L,
    bsr_threshold = 3.0,
    p_threshold = 0.05,
    view_mode = "montage"
  )
}
```

**Note:** Since explore_server calls child module servers, you may need to:
- Test the parent module's reactive logic (LV selection, variance calc)
- Test child modules separately (they have their own test files)
- Use shiny::testServer's module isolation
  </action>
  <verify>
Run `devtools::test(filter = "mod_explore")` — all tests pass
  </verify>
  <done>
test-mod_explore.R exists with testServer() tests covering LV selection, coordination with child modules, and handling of different result types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create mod_brain_viewer testServer tests</name>
  <files>tests/testthat/test-mod_brain_viewer.R</files>
  <action>
Create tests/testthat/test-mod_brain_viewer.R with testServer() tests for the brain viewer module.

**Tests to include:**

1. `describe("brain_viewer_server initialization")`:
   - Default view_mode is "montage"
   - Returns reactive with coord and view_mode

2. `describe("brain_viewer_server view toggle")`:
   - Clicking montage button sets view_mode to "montage"
   - Clicking ortho button sets view_mode to "ortho"
   - View mode syncs from filters if provided

3. `describe("brain_viewer_server with result")`:
   - With valid result and mask, plot renders (no error)
   - With result but no mask, fallback plot renders
   - With NULL result, empty state message shown

4. `describe("brain_viewer_server filter integration")`:
   - LV from filters is used for plotting
   - BSR threshold from filters is used
   - What (bsr/salience) selection works

5. `describe("brain_viewer_server click handling")`:
   - Plot click updates selected_coord
   - Coordinate is accessible from returned reactive

**Test setup:**
```r
# Create mock filters reactive list
make_mock_filters <- function() {
  list(
    lv = reactive({ 1L }),
    bsr_threshold = reactive({ 3.0 }),
    p_threshold = reactive({ 0.05 }),
    view_mode = reactive({ "montage" }),
    what = reactive({ "bsr" })
  )
}

# Result with mask for brain plotting
make_result_with_mask <- function() {
  make_mock_pls_result(
    include_boot = TRUE,
    include_mask = TRUE
  )
}
```

**Challenge:** renderPlot outputs are hard to test directly in testServer. Focus on:
- Reactive state changes (view_mode, selected_coord)
- Input/filter value flow
- Error handling (result is NULL, mask is NULL)
  </action>
  <verify>
Run `devtools::test(filter = "mod_brain_viewer")` — all tests pass
  </verify>
  <done>
test-mod_brain_viewer.R exists with testServer() tests covering view toggle, filter integration, result handling, and click events.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create mod_inspector testServer tests</name>
  <files>tests/testthat/test-mod_inspector.R</files>
  <action>
Create tests/testthat/test-mod_inspector.R with testServer() tests for the inspector module.

**Tests to include:**

1. `describe("inspector_server initialization")`:
   - Module accepts result_rv, selected_lv, state_rv
   - No errors on initialization

2. `describe("inspector_server LV details")`:
   - Displays correct variance explained for selected LV
   - Shows p-value when perm_result exists
   - Shows "N/A" or nothing when perm_result is NULL
   - BSR summary shown when boot_result exists

3. `describe("inspector_server with different results")`:
   - Full result (boot + perm): all stats shown
   - Boot only: BSR stats shown, no p-value
   - Perm only: p-value shown, no BSR stats
   - Basic result: minimal display

4. `describe("inspector_server export buttons")`:
   - Export NIfTI button triggers modal (or handler fires)
   - Export CSV button triggers modal
   - Export PDF button triggers modal
   - Export Report button triggers modal

5. `describe("inspector_server scores plot")`:
   - Plot renders without error for valid result
   - Handles NULL result gracefully
   - Handles NULL selected_lv gracefully

**Test setup:**
```r
# Test all fixture variants
test_that("inspector handles basic result", {
  result <- load_fixture("pls_result_basic")
  # ...
})

test_that("inspector handles full result", {
  result <- load_fixture("pls_result_full")
  # ...
})
```

**Focus areas:**
- Conditional rendering based on result structure
- Export modal triggers (testing actual export is harder)
- Graceful handling of missing data
  </action>
  <verify>
Run `devtools::test(filter = "mod_inspector")` — all tests pass
  </verify>
  <done>
test-mod_inspector.R exists with testServer() tests covering LV details display, conditional content based on result type, and export button handlers.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

```r
# Run all module tests from this plan
devtools::test(filter = "mod_explore|mod_brain|mod_inspector")

# Run ALL module tests (including Plan 03)
devtools::test(filter = "mod_")

# Should see output for all 6 module test files
# All tests should pass
```

Expected: All test files execute without errors, covering the 6 Shiny modules specified in Phase 1 requirements.
</verification>

<success_criteria>
- [ ] test-mod_explore.R exists with 4+ describe blocks
- [ ] test-mod_brain_viewer.R exists with 5+ describe blocks
- [ ] test-mod_inspector.R exists with 5+ describe blocks
- [ ] Tests use fixtures (load_fixture or make_mock_pls_result)
- [ ] Tests cover different result variants (basic, boot, perm, full)
- [ ] devtools::test(filter = "mod_") passes ALL module tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-foundation/01-04-SUMMARY.md`
</output>
