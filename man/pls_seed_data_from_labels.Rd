% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pls_seed_data.R
\name{pls_seed_data_from_labels}
\alias{pls_seed_data_from_labels}
\title{Compute Seed/ROI Data Matrix from a Label Volume}
\usage{
pls_seed_data_from_labels(
  datamat_lst,
  mask,
  labels,
  roi_ids = NULL,
  roi_labels = NULL,
  lags = NULL,
  feature_layout = NULL,
  na_rm = TRUE,
  drop_zero_var = TRUE
)
}
\arguments{
\item{datamat_lst}{List of group matrices (rows = observations).}

\item{mask}{3D brain mask volume/array. Non-zero values define included voxels.}

\item{labels}{3D label volume/array with integer ROI labels (0 = background).}

\item{roi_ids}{Optional integer vector of ROI IDs to extract (defaults to all non-zero labels).}

\item{roi_labels}{Optional character labels for ROIs (defaults to \verb{seed_<id>}).}

\item{feature_layout}{Optional feature layout list (e.g., from \code{pls_result$feature_layout}).}

\item{na_rm}{Logical, passed to \code{rowMeans()}.}

\item{drop_zero_var}{Logical, drop seed columns with zero/NA variance (common for baseline lag).}
}
\value{
Numeric matrix with nrow = sum(nrow(datamat_lst)).
}
\description{
Given voxelwise data matrices and a mask, compute one column per ROI (voxel
layout) or one column per ROI-by-lag (voxel-by-lag layout) as the mean within the
ROI for each observation (row).
}
\keyword{internal}
