% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_projection.R
\name{project_scores}
\alias{project_scores}
\title{Project Held-Out Observations into a Fitted Score Space}
\usage{
project_scores(x, newdata, type = "feature", progress = FALSE, ...)
}
\arguments{
\item{x}{A fitted \code{pls_result} or \code{mva_result}.}

\item{newdata}{Held-out \code{pls_spec} data to project.}

\item{type}{Score space to project into: \code{"feature"}/\code{"brain"},
\code{"design"}/\code{"behavior"}, or \code{"both"}.}

\item{progress}{Logical, show progress.}

\item{...}{Reserved for method-specific extensions.}
}
\value{
A score matrix, or a named list with \code{feature} and \code{design}
matrices when \code{type = "both"}.
}
\description{
Project Held-Out Observations into a Fitted Score Space
}
\examples{
set.seed(42)
datamat <- matrix(rnorm(24 * 10), 24, 10)
spec <- pls_spec()
spec$datamat_lst <- list(datamat)
spec$num_subj_lst <- 12L
spec$num_cond <- 2L
result <- run(spec, progress = FALSE)
proj <- project_scores(result, spec, type = "brain")
}
