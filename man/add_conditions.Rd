% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_conditions.R
\name{add_conditions}
\alias{add_conditions}
\title{Add Conditions to PLS Specification}
\usage{
add_conditions(spec, conditions, labels = NULL)
}
\arguments{
\item{spec}{A \code{pls_spec} object}

\item{conditions}{Either:
\itemize{
\item An integer specifying the number of conditions
\item A character vector of condition labels
\item A data frame with condition information (for BIDS events)
}}

\item{labels}{Optional character vector of condition labels
(if conditions is an integer)}
}
\value{
Updated \code{pls_spec} object
}
\description{
Specifies the condition structure for the PLS analysis.
Conditions define how the data matrix rows are organized.
}
\examples{
set.seed(42)
data1 <- matrix(rnorm(60 * 50), 60, 50)  # 20 subjects x 3 conditions
data2 <- matrix(rnorm(54 * 50), 54, 50)  # 18 subjects x 3 conditions

spec <- pls_spec() |>
  add_subjects(list(data1, data2), groups = c(20, 18)) |>
  add_conditions(3, labels = c("baseline", "task1", "task2"))
}
