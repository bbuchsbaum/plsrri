% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample_splithalf.R
\name{pls_splithalf_test}
\alias{pls_splithalf_test}
\title{Run Split-Half Validation}
\usage{
pls_splithalf_test(
  stacked_datamat,
  stacked_behavdata = NULL,
  stacked_designdata = NULL,
  num_groups,
  num_subj_lst,
  num_cond,
  method,
  num_split,
  num_outer_perm = 0L,
  clim = 95,
  bscan = NULL,
  meancentering_type = 0L,
  cormode = 0L,
  is_struct = FALSE,
  outer_reorder = NULL,
  inner_subject_perms = NULL,
  progress = TRUE
)
}
\arguments{
\item{stacked_datamat}{Stacked data matrix}

\item{stacked_behavdata}{Behavior data matrix (methods 3-6)}

\item{stacked_designdata}{Design contrast matrix (methods 2, 5, 6)}

\item{num_groups}{Number of groups}

\item{num_subj_lst}{Subjects per group}

\item{num_cond}{Number of conditions}

\item{method}{PLS method (1-6)}

\item{num_split}{Number of split-half iterations}

\item{num_outer_perm}{Number of outer permutations for significance
(includes the unpermuted reference sample as the first permutation,
MATLAB \code{missnk_rri_perm_order} style).}

\item{clim}{Confidence level used for optional CI reporting (percent).}

\item{bscan}{Conditions for behavior block}

\item{meancentering_type}{Mean-centering type}

\item{cormode}{Correlation mode}

\item{is_struct}{Structure PLS flag (do not permute conditions within-subject)}

\item{outer_reorder}{Optional matrix of outer permutation orders
(\code{nrow(stacked_datamat)} x \code{num_outer_perm}). If provided, overrides the
internally generated outer permutation orders (MATLAB \code{permsamp} style).}

\item{inner_subject_perms}{Optional nested list specifying the within-group
subject permutations for each outer permutation and split. If provided,
it must have shape \verb{[[op]][[p]][[g]]} where each entry is a permutation of
\code{1:num_subj_lst[g]}. This enables deterministic split-half runs for testing.}

\item{progress}{Show progress}
}
\value{
pls_splithalf_result object
}
\description{
Performs split-half cross-validation where the sample is randomly split,
PLS is run on each half, and the correlation between the resulting
weights (u) and loadings (v) is computed.
}
