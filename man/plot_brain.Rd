% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_brain.R
\name{plot_brain}
\alias{plot_brain}
\title{Plot Brain Salience or BSR Map}
\usage{
plot_brain(
  x,
  lv = 1,
  what = "bsr",
  lag = NULL,
  threshold = NULL,
  view = "montage",
  background = NULL,
  slices = NULL,
  along = 3L,
  cmap = "inferno",
  ncol = 6L,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{pls_result} object}

\item{lv}{Latent variable to plot (default 1)}

\item{what}{What to plot: "salience" or "bsr"}

\item{lag}{Optional lag label to plot for voxel×lag feature layouts (e.g., 0).}

\item{threshold}{Threshold for BSR (e.g., 3 for |BSR| > 3)}

\item{view}{View type: "montage" (default), "ortho", or "overlay"}

\item{background}{For "overlay" view: a NeuroVol to use as background.
If NULL and overlay is requested, falls back to montage.}

\item{slices}{Slice indices (NULL = auto, passed to neuroim2)}

\item{along}{Axis for slicing (3 = axial, 2 = coronal, 1 = sagittal)}

\item{cmap}{Color map name (default "inferno" for statistical maps)}

\item{ncol}{Number of columns for montage layout}

\item{title}{Plot title (NULL = auto-generate)}

\item{...}{Additional arguments passed to neuroim2 plotting functions}
}
\value{
A ggplot object
}
\description{
Creates brain slice plots for PLS saliences or bootstrap ratios.
Uses neuroim2's plotting functions for visualization.
}
\examples{
\dontrun{
result <- quick_pls(list(d1, d2), c(20, 18), 3, nboot = 100)
plot_brain(result, lv = 1, what = "bsr", threshold = 3)
plot_brain(result, lv = 1, what = "bsr", view = "ortho")
}
}
