% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_subjects.R
\name{add_subjects}
\alias{add_subjects}
\title{Add Subjects to PLS Specification}
\usage{
add_subjects(
  spec,
  data,
  groups = NULL,
  task = NULL,
  space = "MNI152NLin2009cAsym",
  mask_method = "intersection",
  ...
)
}
\arguments{
\item{spec}{A \code{pls_spec} object}

\item{data}{Either:
\itemize{
\item A character path to a BIDS directory
\item A list of data matrices (one per group)
\item A single data matrix (one group)
}}

\item{groups}{Character vector of group labels (for BIDS) or
integer vector of subjects per group (for matrices)}

\item{task}{Task name (for BIDS)}

\item{space}{Space name for fMRIPrep derivatives (default "MNI152NLin2009cAsym")}

\item{mask_method}{How to handle brain mask:
\itemize{
\item "intersection": use intersection of all subject masks
\item "union": use union of all subject masks
\item "first": use first subject's mask
\item "provided": use mask from pls_spec
}}

\item{...}{Additional arguments passed to bidser functions}
}
\value{
Updated \code{pls_spec} object
}
\description{
Adds subject data to a PLS specification. Can load from BIDS directories
(if bidser is available) or accept pre-loaded data matrices.
}
\examples{
# From pre-loaded matrices
set.seed(42)
group1_data <- matrix(rnorm(60 * 50), 60, 50)
group2_data <- matrix(rnorm(54 * 50), 54, 50)

spec <- pls_spec() |>
  add_subjects(list(group1_data, group2_data), groups = c(20, 18))

# From BIDS (requires bidser)
\dontrun{
spec <- pls_spec() |>
  add_subjects("/path/to/bids", groups = c("control", "patient"), task = "rest")
}
}
