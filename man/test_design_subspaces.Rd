% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_subspace.R
\name{test_design_subspaces}
\alias{test_design_subspaces}
\title{Test Task PLS Design Subspaces}
\usage{
test_design_subspaces(
  x,
  fit = NULL,
  spec = NULL,
  design = NULL,
  formula = NULL,
  condition_key = NULL,
  terms = "all",
  statistic = c("trace", "largest_root"),
  weights = c("cell_equal", "subject_count", "row_weights"),
  nperm = 0L,
  permutation = c("none", "global_task_pls"),
  correction = c("none", "maxT"),
  permsamp = NULL,
  progress = FALSE
)
}
\arguments{
\item{x}{A Task PLS \code{pls_result}, or a \code{pls_spec} when \code{fit} is supplied.}

\item{fit}{Optional fitted \code{pls_result} when \code{x} is a \code{pls_spec}.}

\item{spec}{Optional \code{pls_spec} used to recompute permuted cross-block
matrices. Required for \code{nperm > 0} when \code{x} is a \code{pls_result}.}

\item{design}{Optional \code{pls_design} object.}

\item{formula}{Optional one-sided formula overriding \code{design$formula}.}

\item{condition_key}{Optional condition metadata.}

\item{terms}{\code{"all"} or a character vector of term labels to test.}

\item{statistic}{\code{"trace"} for total subspace covariance energy, or
\code{"largest_root"} for the dominant singular-root statistic.}

\item{weights}{\code{"cell_equal"}, \code{"subject_count"}, or \code{"row_weights"} to
use stored \code{result$task_pls$row_weights}.}

\item{nperm}{Number of permutations.}

\item{permutation}{\code{"none"} or \code{"global_task_pls"}.}

\item{correction}{\code{"none"} or \code{"maxT"} adjustment across tested terms.}

\item{permsamp}{Optional permutation order matrix.}

\item{progress}{Logical; show permutation progress messages.}
}
\value{
A data frame with term, rank, statistic, p-value, and null labels.
}
\description{
Computes observed covariance energy in term-specific design subspaces and,
when requested, compares those statistics to a global Task PLS permutation
null. The permutation null asks whether term-aligned covariance is larger
than expected under the package's ordinary global Task PLS row permutation.
}
