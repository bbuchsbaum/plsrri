% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_subspace.R
\name{test_design_terms}
\alias{test_design_terms}
\title{Observed Design-Subspace Statistics for Task PLS Terms}
\usage{
test_design_terms(
  result,
  design = NULL,
  formula = NULL,
  condition_key = NULL,
  statistic = c("trace", "largest_root"),
  weights = c("cell_equal", "subject_count", "row_weights"),
  nperm = 0L,
  correction = c("none", "maxT")
)
}
\arguments{
\item{result}{A Task PLS \code{pls_result} produced with
\code{pls_analysis(..., keep_crossblock = TRUE)} or
\code{run(..., keep_crossblock = TRUE)}.}

\item{design}{Optional \code{pls_design} object.}

\item{formula}{Optional one-sided formula overriding \code{design$formula}.}

\item{condition_key}{Optional condition metadata.}

\item{statistic}{\code{"trace"} for total subspace covariance energy, or
\code{"largest_root"} for the dominant singular-root statistic.}

\item{weights}{\code{"cell_equal"}, \code{"subject_count"}, or \code{"row_weights"} to
use stored \code{result$task_pls$row_weights}.}

\item{nperm}{Number of permutations. Permutation inference is not yet
implemented for design-subspace tests; values greater than zero error.}

\item{correction}{Multiple-testing correction label. Present for API
compatibility with future permutation support.}
}
\value{
A data frame with term, rank, statistic, and placeholder p-value
columns.
}
\description{
Computes observed covariance energy in each factorial design subspace. The
subspaces are built in the centered Task PLS row space, so effects removed
by the Task PLS mean-centering operator have rank zero.
}
