% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_run.R
\name{run}
\alias{run}
\title{Run PLS Analysis from Specification}
\usage{
run(spec, progress = TRUE, ...)
}
\arguments{
\item{spec}{A \code{pls_spec} object}

\item{progress}{Logical, show progress messages}

\item{...}{Additional arguments passed to \code{pls_analysis()}}
}
\value{
A \code{pls_result} object
}
\description{
Validates the PLS specification and executes the analysis.
This is the final step in the builder pattern.
}
\examples{
set.seed(42)
data1 <- matrix(rnorm(60 * 50), 60, 50)
data2 <- matrix(rnorm(54 * 50), 54, 50)

result <- pls_spec() |>
  add_subjects(list(data1, data2), groups = c(20, 18)) |>
  add_conditions(3) |>
  configure(method = "task", nperm = 10) |>
  run(progress = FALSE)
}
