% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_design.R
\name{plot_design_score_space}
\alias{plot_design_score_space}
\title{Plot a Two-LV Design Score Space}
\usage{
plot_design_score_space(
  x,
  lv = c(1L, 2L),
  condition_key = NULL,
  formula = NULL,
  label = NULL,
  label_sep = "\\n",
  color = NULL,
  shape = NULL,
  facet = NULL,
  show_origin = TRUE,
  show_segments = TRUE,
  show_se = FALSE,
  aspect = c("fixed", "free"),
  padding = 0.2,
  title = NULL,
  block = NULL
)
}
\arguments{
\item{x}{A \code{pls_result} object.}

\item{lv}{Integer vector of two latent variables.}

\item{condition_key}{Optional condition metadata.}

\item{formula}{Optional one-sided formula controlling aggregation and
faceting. Variables before \code{|} identify plotted points; variables after
\code{|} identify facets. Variables omitted from the formula are averaged over.
For example, \code{~ group + condition | task} labels group-condition centroids
in task facets, while \code{~ group | task} collapses over condition labels
inside each task.}

\item{label}{Character vector of columns to paste into point labels. Defaults
to \code{group} and \code{condition} when a group column is available.}

\item{label_sep}{Separator used when \code{label} has multiple columns.}

\item{color}{Optional column name for point color. Defaults to \code{group} when
available.}

\item{shape}{Optional column name for point shape.}

\item{facet}{Optional column name or character vector used for faceting.
Defaults to the variables after \code{|} in \code{formula}.}

\item{show_origin}{Logical; draw zero reference lines.}

\item{show_segments}{Logical; draw segments from the origin to each centroid.}

\item{show_se}{Logical; draw standard-error bars in both LV dimensions.}

\item{aspect}{Aspect handling. \code{"fixed"} uses the same numeric scale for
both axes; \code{"free"} lets the panel fill the available device.}

\item{padding}{Fractional padding added around the joint x/y range. This
keeps labels visible when points sit near the edge of the score space.}

\item{title}{Optional plot title.}

\item{block}{Optional score block to keep for multiblock results.}
}
\value{
A ggplot object.
}
\description{
Plot group-condition design-score centroids with one latent variable on each
axis. Labels identify the design cells, while colors or shapes can encode
grouping factors.
}
