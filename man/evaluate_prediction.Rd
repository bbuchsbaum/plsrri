% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_cv.R
\name{evaluate_prediction}
\alias{evaluate_prediction}
\title{Evaluate Predictive Performance with Nested Cross-Validation}
\usage{
evaluate_prediction(
  spec,
  outcome,
  task = c("classification", "regression"),
  components = NULL,
  resampling = NULL,
  metrics = NULL,
  primary_metric = NULL,
  seed = NULL,
  progress = TRUE,
  num_perm = 0L,
  num_boot = 0L,
  clim = 95
)
}
\arguments{
\item{spec}{A \code{pls_spec} object.}

\item{outcome}{Subject-level outcome vector ordered by group, then subject.}

\item{task}{\code{"classification"} or \code{"regression"}.}

\item{components}{Optional integer vector of candidate LV counts. When NULL,
each outer split tunes over \code{1:min(5, available_components)}.}

\item{resampling}{Optional list controlling nested CV. Supported fields are
\code{outer_folds}, \code{outer_repeats}, \code{inner_folds}, \code{inner_repeats},
\code{stratify}, plus shorthand \code{folds}/\code{repeats}.}

\item{metrics}{Optional character vector of metric names.}

\item{primary_metric}{Optional metric used for inner-loop selection.}

\item{seed}{Optional integer seed for reproducible split generation.}

\item{progress}{Logical; show a progress bar.}

\item{num_perm}{Optional number of subject-level label permutations for
predictive inference.}

\item{num_boot}{Optional number of subject-level bootstrap resamples for
predictive confidence intervals.}

\item{clim}{Confidence level for bootstrap intervals.}
}
\value{
A \code{predict_cv_result} list with out-of-fold predictions, per-fold
metrics, split membership, and tuning traces.
}
\description{
Evaluate Predictive Performance with Nested Cross-Validation
}
