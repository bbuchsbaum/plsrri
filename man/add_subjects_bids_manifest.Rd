% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_bids_manifest.R
\name{add_subjects_bids_manifest}
\alias{add_subjects_bids_manifest}
\title{Add Subjects from BIDS as a Lagged NIfTI Manifest}
\usage{
add_subjects_bids_manifest(
  spec,
  bids_dir,
  groups = NULL,
  group_col = NULL,
  task = NULL,
  space = NULL,
  file_regex = ".*\\\\.nii(\\\\.gz)?$",
  condition_keys = "cond",
  volumes = NULL,
  mask_method = "intersection",
  strict = TRUE,
  ...
)
}
\arguments{
\item{spec}{A \code{pls_spec} object.}

\item{bids_dir}{Path to BIDS dataset root.}

\item{groups}{NULL/"all" or character vector of group labels found in
\code{participants.tsv} (same semantics as \code{add_subjects()}).}

\item{group_col}{Optional grouping column name in \code{participants.tsv}. When
provided, \code{groups} are interpreted as values from this column.}

\item{task}{Optional task filter passed to \code{bidser::search_files()}.}

\item{space}{Optional space filter passed to \code{bidser::search_files()} and
\code{bidser::mask_files()}.}

\item{file_regex}{Regex applied to filenames to find candidate NIfTI files.
Defaults to any NIfTI.}

\item{condition_keys}{Character vector of BIDS entity keys to use to define
conditions. Values are extracted from filenames using patterns like
\verb{_<key>-<value>} and joined with \verb{_}.}

\item{volumes}{Optional integer vector (1-based) or compact spec string
(e.g., \code{"1:8"}) selecting a subset of dim4 volumes (lags). When provided,
the manifest \code{file} column uses \code{file[volspec]} syntax.}

\item{mask_method}{How to derive a brain mask if \code{spec$mask} is NULL:
\code{"intersection"} (default), \code{"union"}, or \code{"first"}.}

\item{strict}{Logical. If TRUE (default), error on missing condition keys or
duplicated (group, subject, condition) rows.}

\item{...}{Additional filters forwarded to \code{bidser::search_files()} and
\code{bidser::mask_files()}.}
}
\value{
Updated \code{pls_spec} object with manifest-based inputs.
}
\description{
Builds a manifest (subject × condition rows) from a BIDS dataset using
\code{bidser}, then routes through \code{add_subjects_manifest()} so that \strong{NIfTI dim4}
is interpreted as \strong{lags} and folded as voxel×lag features.

This is intended for event-related/fIR-style derivatives where each file is a
4D NIfTI whose 4th dimension corresponds to lags (time since event onset).
}
