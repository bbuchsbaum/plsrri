% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_subspace.R
\name{compare_design_subspaces}
\alias{compare_design_subspaces}
\title{Compare Nested Task PLS Design Subspaces}
\usage{
compare_design_subspaces(
  x,
  reduced,
  full,
  fit = NULL,
  spec = NULL,
  design = NULL,
  condition_key = NULL,
  statistic = c("trace", "largest_root"),
  weights = c("cell_equal", "subject_count", "row_weights"),
  nperm = 0L,
  permutation = c("none", "global_task_pls", "reduced_model"),
  permsamp = NULL,
  progress = FALSE
)
}
\arguments{
\item{x}{A Task PLS \code{pls_result}, or a \code{pls_spec} when \code{fit} is supplied.}

\item{reduced}{One-sided reduced formula.}

\item{full}{One-sided full formula.}

\item{fit}{Optional fitted \code{pls_result} when \code{x} is a \code{pls_spec}.}

\item{spec}{Optional \code{pls_spec} used to recompute permuted cross-block
matrices. Required for \code{nperm > 0} when \code{x} is a \code{pls_result}.}

\item{design}{Optional \code{pls_design} object.}

\item{condition_key}{Optional condition metadata.}

\item{statistic}{\code{"trace"} or \code{"largest_root"}.}

\item{weights}{\code{"cell_equal"}, \code{"subject_count"}, or \code{"row_weights"} to
use stored \code{result$task_pls$row_weights}.}

\item{nperm}{Number of permutations.}

\item{permutation}{\code{"none"} or \code{"global_task_pls"}.}

\item{permsamp}{Optional permutation order matrix.}

\item{progress}{Logical; show permutation progress messages.}
}
\value{
A one-row data frame with observed nested-subspace statistic.
}
\description{
Fits the delta design subspace from a full cell-level design after QR
residualization against a reduced design. With
\code{permutation = "global_task_pls"}, compares the delta statistic to the
package's ordinary global Task PLS permutation null. This global null is not
a reduced-model-preserving nested test.
}
