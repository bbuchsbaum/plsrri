% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_run.R
\name{add_trial_data}
\alias{add_trial_data}
\title{Add Trial-Level Data for Within-Subject Seed PLS}
\usage{
add_trial_data(
  spec,
  beta_lst,
  seed_lst,
  condition_lst,
  groups = NULL,
  seed_labels = NULL,
  layout = c("seed_condition", "stacked_seed_features"),
  fisher_z = TRUE,
  min_trials = 3L
)
}
\arguments{
\item{spec}{A \code{pls_spec} object.}

\item{beta_lst}{A list of matrices (one per subject), each with
rows = trials and columns = voxels.}

\item{seed_lst}{A list of numeric vectors or matrices (one per subject)
with the seed region's trial-level betas.}

\item{condition_lst}{A list of integer vectors or factors (one per subject)
mapping trials to conditions.}

\item{fisher_z}{Logical; apply Fisher r-to-z transform (default \code{TRUE}).}

\item{min_trials}{Minimum trials per condition for valid correlation
(default 3).}
}
\value{
Updated \code{pls_spec} object.
}
\description{
Attaches trial-level beta matrices, seed data, and condition mappings
to a PLS specification for use with \code{method = "ws_seed"}.
}
\examples{
\dontrun{
result <- pls_spec() |>
  add_trial_data(beta_lst, seed_lst, cond_lst) |>
  configure(method = "ws_seed", nperm = 500, nboot = 500) |>
  run()
}
}
