% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_design.R
\name{as_design_contrasts}
\alias{as_design_contrasts}
\title{Factorial Design Contrasts for a Latent Variable}
\usage{
as_design_contrasts(
  x,
  lv = 1L,
  condition_key = NULL,
  factors = NULL,
  block = NULL
)
}
\arguments{
\item{x}{A \code{pls_result} object.}

\item{lv}{Latent variable to summarize.}

\item{condition_key}{Optional data frame with one row per condition and a
\code{condition} column.}

\item{factors}{Character vector of factor columns to include. Defaults to
\code{group} plus every column supplied by \code{condition_key} except \code{condition}.}

\item{block}{Optional score block to keep for multiblock results.}
}
\value{
A data frame with one row per factorial term.
}
\description{
Decompose the summarized LV design scores into an effect-coded factorial
model. The returned magnitudes show how much of the flattened design-score
pattern lies in each main-effect or interaction subspace.
}
