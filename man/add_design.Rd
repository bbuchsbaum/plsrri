% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_behavior.R
\name{add_design}
\alias{add_design}
\title{Add Design Contrasts to PLS Specification}
\usage{
add_design(spec, contrasts, labels = NULL)
}
\arguments{
\item{spec}{A \code{pls_spec} object}

\item{contrasts}{Contrast matrix where:
\itemize{
\item For method 2 (non-rotated task): rows = groups x conditions
\item For method 5 (non-rotated behavior): rows = groups x conditions x measures
\item For method 6 (non-rotated multiblock): rows = task rows + behavior rows
\item Columns = number of contrasts/LVs to extract
}}

\item{labels}{Optional character vector of contrast labels}
}
\value{
Updated \code{pls_spec} object
}
\description{
Adds design contrasts for non-rotated PLS methods.
Design contrasts specify hypotheses about condition/group differences.
}
\examples{
# Create brain data for 2 groups
set.seed(42)
d1 <- matrix(rnorm(60 * 50), 60, 50)  # 20 subjects x 3 conditions
d2 <- matrix(rnorm(54 * 50), 54, 50)  # 18 subjects x 3 conditions

# 2 groups x 3 conditions = 6 rows, testing 2 contrasts
contrasts <- matrix(c(
  1, 0,   # g1c1
  0, 1,   # g1c2
 -1,-1,   # g1c3
  1, 0,   # g2c1
  0, 1,   # g2c2
 -1,-1    # g2c3
), ncol = 2, byrow = TRUE)

spec <- pls_spec() |>
  add_subjects(list(d1, d2), groups = c(20, 18)) |>
  add_conditions(3) |>
  add_design(contrasts, labels = c("task_vs_baseline", "task1_vs_task2"))
}
