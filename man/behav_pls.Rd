% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/builder_run.R
\name{behav_pls}
\alias{behav_pls}
\title{Behavioral Correlates PLS}
\usage{
behav_pls(
  datamat_lst,
  behav_data,
  num_subj_lst,
  num_cond,
  cormode = 0L,
  nperm = 1000,
  nboot = 500,
  ...
)
}
\arguments{
\item{datamat_lst}{List of data matrices (one per group).}

\item{behav_data}{Matrix of behavioral measures. Rows must match stacked
datamat, columns are different behavioral measures.}

\item{num_subj_lst}{Integer vector with number of subjects per group.}

\item{num_cond}{Number of conditions.}

\item{cormode}{Correlation mode (0=Pearson, 2=covariance, 4=cosine, 6=dot).}

\item{nperm}{Number of permutations (default 1000).}

\item{nboot}{Number of bootstrap samples (default 500).}

\item{...}{Additional arguments passed to \code{pls_analysis()}.}
}
\value{
A \code{pls_result} object with class \code{pls_behavior}.
}
\description{
Convenience function for behavior PLS analysis, identifying brain patterns
that correlate with behavioral measures (reaction time, accuracy, etc.).
}
\examples{
\dontrun{
# Behavioral measures: RT and accuracy
behav <- cbind(rt = rt_data, accuracy = acc_data)

result <- behav_pls(
  datamat_lst = list(brain_data),
  behav_data = behav,
  num_subj_lst = 30,
  num_cond = 4
)
}
}
