# Read a `plscli` Pipeline Specification

Read a `plscli` Pipeline Specification

## Usage

``` r
read_pipeline_spec(x)
```

## Arguments

- x:

  Path to a YAML file or an already-loaded list.

## Value

A normalized pipeline specification list.
