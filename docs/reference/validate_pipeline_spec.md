# Validate a `plscli` Pipeline Specification

Validate a `plscli` Pipeline Specification

## Usage

``` r
validate_pipeline_spec(x)
```

## Arguments

- x:

  Path to a YAML file or a specification list.

## Value

The validated specification with defaults applied.
