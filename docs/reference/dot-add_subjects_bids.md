# Add Subjects from BIDS Directory

Loads subject data from a BIDS directory using bidser. Supports fMRIPrep
preprocessed derivatives.

## Usage

``` r
.add_subjects_bids(spec, bids_dir, groups, task, space, mask_method, ...)
```
