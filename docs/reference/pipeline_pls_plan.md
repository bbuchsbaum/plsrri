# Build a PLS Observation Manifest from First-Level Outputs

Build a PLS Observation Manifest from First-Level Outputs

## Usage

``` r
pipeline_pls_plan(spec)
```

## Arguments

- spec:

  Path to a YAML file or a pipeline specification list.

## Value

The PLS manifest as a data.frame.
