# Builder API: Add Subjects from Map Manifest

Helpers for constructing PLS-ready data matrices from manifests where
each observation references a single 3D map (or a single selected volume
from a 4D image). This is the primary intake path for first-level GLM
estimate maps.
