# Core Bootstrap Order Generator (No bscan mapping)

Core Bootstrap Order Generator (No bscan mapping)

## Usage

``` r
.pls_boot_order_core(
  num_subj_lst,
  num_cond,
  num_boot,
  incl_seq = FALSE,
  boot_type = "strat"
)
```
