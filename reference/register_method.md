# Register a Multivariate Analysis Method

Register a Multivariate Analysis Method

## Usage

``` r
register_method(name, method, aliases = character(0))
```

## Arguments

- name:

  Canonical method name

- method:

  An `MvaMethod` R6 instance

- aliases:

  Character vector of alternative names
