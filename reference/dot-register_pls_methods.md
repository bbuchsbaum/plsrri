# Register all PLS method variants

Called during package load to register all 6 PLS variants in the method
registry. Each gets its canonical name plus aliases for backward
compatibility.

## Usage

``` r
.register_pls_methods()
```
