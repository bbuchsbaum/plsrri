# Run the `plscli` Dispatcher

Run the `plscli` Dispatcher

## Usage

``` r
plscli_main(args = commandArgs(trailingOnly = TRUE))
```

## Arguments

- args:

  Character vector of command-line arguments.

## Value

Invisibly returns an integer exit status.
