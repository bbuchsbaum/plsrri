# Get Number of Components

Returns the number of components (latent variables, principal
components, independent components, etc.) in the result.

## Usage

``` r
n_components(x, ...)
```

## Arguments

- x:

  An `mva_result` or `pls_result` object

- ...:

  Additional arguments

## Value

Integer
