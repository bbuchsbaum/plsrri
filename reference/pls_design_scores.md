# Compute Design Scores

Computes design scores by projecting the design matrix onto the design
loadings (v from SVD).

## Usage

``` r
pls_design_scores(design, v)
```

## Arguments

- design:

  Design matrix

- v:

  Design loadings from SVD (v matrix)

## Value

Design scores matrix
